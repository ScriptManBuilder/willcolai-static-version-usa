# ✅ Начальный Loader - Умная Загрузка Сайта

## 🎯 Зачем это нужно?

**Проблема:** При первой загрузке Home.tsx анимации показываются с задержкой, потому что:
- Тяжёлая галерея с изображениями
- Видео в фоне
- Множество анимаций
- Ресурсы загружаются асинхронно

**Решение:** Показываем красивый loader пока всё не загрузится!

---

## ✨ Что я сделал?

### 1. Создал хук `useInitialLoader`

**Файл:** `src/hooks/useInitialLoader.ts`

**Функции:**
- ✅ Проверяет, первый ли это запуск (через `sessionStorage`)
- ✅ Эмулирует загрузку с плавным прогрессом (0% → 100%)
- ✅ Ждёт полной загрузки документа (`document.readyState === 'complete'`)
- ✅ Минимум 2 секунды, максимум 4 секунды
- ✅ При повторных заходах **НЕ показывается** (только в новой вкладке/сессии)

### 2. Создал компонент `InitialLoader`

**Файл:** `src/components/InitialLoader.tsx`

**Дизайн:**
- 🎨 Красивый gradient фон (как в Auth loader)
- ⚡ Анимированный спиннер
- 📊 Прогресс-бар с shimmer эффектом
- 💬 Динамический текст в зависимости от прогресса:
  - 0-30%: "Initializing..."
  - 30-60%: "Loading resources..."
  - 60-90%: "Almost ready..."
  - 90-100%: "Finalizing..."
- ✨ Плавное исчезновение (fade out)

### 3. Интегрировал в `App.tsx`

**Логика:**
```
1. Первая загрузка? → InitialLoader ✅
2. Загружено → проверка Auth
3. Auth loading? → Auth loader ✅
4. Всё готово → показ сайта ✅
```

---

## 🎬 Как это работает?

### Первый запуск (новая вкладка):

```
Пользователь открывает сайт
        ↓
InitialLoader показывается
        ↓
Прогресс: 0% → 100% (2-4 сек)
        ↓
Плавное исчезновение
        ↓
Сайт полностью загружен!
```

### Повторный заход (в той же вкладке):

```
Пользователь переходит на другую страницу
        ↓
Возвращается на Home
        ↓
InitialLoader НЕ показывается ✅
        ↓
Мгновенная загрузка!
```

### Новая вкладка:

```
Пользователь открывает в новой вкладке
        ↓
InitialLoader показывается снова ✅
        ↓
Прогресс → загрузка
```

---

## ⚙️ Технические детали:

### SessionStorage:
```javascript
sessionStorage.setItem('siteInitiallyLoaded', 'true')
```
- Сохраняется только на время сессии (вкладки)
- При закрытии вкладки → удаляется автоматически
- При новой вкладке → loader показывается снова

### Минимальное/Максимальное время:
```javascript
const minLoadingTime = 2000; // 2 секунды минимум
const maxLoadingTime = 4000; // 4 секунды максимум
```
- Слишком быстро = пользователь не понимает что происходит
- Слишком долго = раздражает
- 2-4 секунды = оптимально ✅

### Прогресс:
```javascript
setLoadingProgress((prev) => prev + Math.random() * 15)
```
- Случайное увеличение для реалистичности
- Останавливается на 90%
- Доводится до 100% только когда `document.readyState === 'complete'`

---

## 🎨 Визуальные эффекты:

### 1. Pulse анимация (пульсация)
```css
animation: pulse 2s ease-in-out infinite;
```
Логотип плавно увеличивается/уменьшается

### 2. Spinner (вращение)
```css
animation: spin 1s linear infinite;
```
Круговой индикатор загрузки

### 3. Shimmer (переливание)
```css
animation: shimmer 2s linear infinite;
```
Прогресс-бар переливается

### 4. Fade Out (исчезновение)
```css
animation: fadeOut 0.5s ease-out forwards;
```
Плавное исчезновение при 100%

---

## 📱 Адаптивность:

### Desktop:
- Логотип: 3rem
- Прогресс-бар: 300px

### Tablet (≤768px):
- Логотип: 2.5rem
- Прогресс-бар: 250px

### Mobile (≤480px):
- Логотип: 2rem
- Прогресс-бар: 200px

---

## 🧪 Тестирование:

### Как проверить:

1. **Первая загрузка:**
   ```
   - Откройте сайт в новой вкладке
   - Должен показаться InitialLoader
   - Прогресс 0% → 100%
   - Плавное исчезновение
   - Сайт показывается
   ```

2. **Повторный заход:**
   ```
   - Перейдите на /products
   - Вернитесь на /
   - InitialLoader НЕ должен показаться ✅
   ```

3. **Новая вкладка:**
   ```
   - Откройте в новой вкладке (Ctrl+Click)
   - InitialLoader должен показаться снова ✅
   ```

4. **Закрытие/Открытие:**
   ```
   - Закройте вкладку
   - Откройте сайт снова
   - InitialLoader должен показаться ✅
   ```

---

## 💡 Преимущества:

✅ **Профессиональный вид** - пользователь видит, что сайт загружается
✅ **Скрывает задержки** - анимации Home.tsx загружаются незаметно
✅ **Не раздражает** - показывается только при первой загрузке
✅ **Красиво** - gradient, анимации, прогресс
✅ **Адаптивно** - работает на всех устройствах
✅ **Умно** - использует sessionStorage для оптимизации

---

## 🎯 Итог:

### Было:
❌ Первая загрузка → анимации показываются с задержкой
❌ Пользователь видит "недогруженный" сайт
❌ Непрофессионально

### Стало:
✅ Первая загрузка → красивый loader
✅ Плавный переход к контенту
✅ Всё анимации готовы
✅ Профессионально! 🎉

---

**Новые файлы:**
1. ✅ `src/hooks/useInitialLoader.ts` - логика загрузки
2. ✅ `src/components/InitialLoader.tsx` - UI компонент
3. ✅ `src/App.tsx` - интеграция (обновлён)

**Готово! Теперь первая загрузка выглядит профессионально!** 🚀
